1- Import problem accross dir:

        Python only searches for modules in directories listed in sys.path,
        which usually includes the current script’s folder but not its parent directories.


        When running pytest, the tests folder becomes the working directory, so Python can’t see modules in sibling folders like models.


        Adding __init__.py or modifying sys.path tells Python to treat the project as a package, allowing cross-folder imports.

2-how to create a venv with latest python version using git bash:

        py -3.13 -m venv testingenv313

        testingenv313/Scripts/activate
        
        python backend/true_caller_api/manage.py runserver

        python --version

        Python 3.13.2

3- Dealing with git tracking python cache files:

        just add dirname/pattern*  to gitignore file 

        ex:
                ignore/
                models/__*



4- Project Restructuring (Import Fixes):

        Moved "tests" -> "backend/true_caller_api/check_tests" so they are inside the Django project.
        Moved "models/account.py" out of root to avoid conflicts.
        Fixed import errors by organizing files where Python/Django expects them.

5- Environment Setup:

        Created "requirements.txt" with:
        django, djangorestframework, pytest, pytest-django.
        Run "pip install -r requirements.txt" to install them.

6- Pytest Configuration (pytest.ini):

        Created "backend/true_caller_api/pytest.ini".
        "DJANGO_SETTINGS_MODULE = true_caller_api.settings": Tells pytest which settings to use.
        "testpaths = check_tests": Tells pytest where to look for tests.


7- Detailed Backend Change Log (API & Configuration):

    A. Configuration (backend/true_caller_api/true_caller_api/settings.py):
       - Added `testserver` and `127.0.0.1` to `ALLOWED_HOSTS` for verification.
       - Configured `REST_FRAMEWORK` throttling:
         - `DEFAULT_THROTTLE_CLASSES`: Added `UserRateThrottle`.
         - `DEFAULT_THROTTLE_RATES`: Set `user` scope to `5/hour`.

    B. Database Models (backend/true_caller_api/true_caller_app/models.py):
       - Modified `Contact` model:
         - Added `carrier` field (CharField, max_length=50, nullable).

    C. Serializers (backend/true_caller_api/true_caller_app/serializers.py):
       - Modified `ContactSerializer`:
         - Included `'carrier'` in the `fields` list to expose it in API responses.

    D. Views (backend/true_caller_api/true_caller_app/views.py):
       - Validated `SearchView`.
       - Documented usage of default `UserRateThrottle` (implicit via settings).

    E. Admin Panel (backend/true_caller_api/true_caller_app/admin.py):
       - Registered `Contact`, `SpamReport`, and `UserProfile` models.
       - Implemented `ModelAdmin` classes for enhanced display (list_display, search_fields, list_filter).

    F. Migrations (backend/true_caller_api/true_caller_app/migrations/):
       - Created and applied `0002_contact_spamreport_userprofile_delete_phone.py` to persist schema changes.

8- Test Data Generation & Loading:

    A. Data Source (edge_case_data.csv):
       - Created a CSV file with 10+ records covering edge cases:
         - Max length strings.
         - Special characters and SQL injection attempts.
         - Unicode characters.
         - Empty fields and zero values.

    B. Management Command (backend/true_caller_api/true_caller_app/management/commands/load_test_data.py):
       - Implemented a custom Django management command `load_test_data`.
       - Reads the CSV and populates the `Contact` model.
       - Handles updates (idempotent based on phone number) and reports errors.

    C. Usage:
       Run the following command from the project root:
       `python backend/true_caller_api/manage.py load_test_data edge_case_data.csv`
